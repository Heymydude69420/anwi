node_modules
dist
.DS_Store
*.local

# The camera originals only. Anchored with a leading slash so it matches the
# root folder alone — a bare "photos/" also matches public/photos, which would
# silently keep the optimized set that the gallery actually loads out of git.
/photos/
